# 环境变量
.env

# 会话数据（旧单会话文件所在目录，保留忽略以防残留）
data/

# Agent 定义与各自的会话（人设 + 白名单 + 会话历史，属本机私有数据）
# 想让人设 / 白名单进仓库的话，把这行换成只忽略会话文件：
#   agents/*/session.jsonl
agents/

# Skills（本地技能，不上传）
skills/

# Skills 迁移备份（搬家前的逐字节副本，保留 2 周后手动删除）
skills_bak_20260915/

# 文档（用户本地文档，不上传）
documents/

# 向量知识库
vector_store/

# Python 缓存
__pycache__/
*.pyc
*.pyo
*/__pycache__/

# IDE
.vscode/
.idea/
