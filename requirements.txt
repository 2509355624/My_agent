flask>=2.0
requests>=2.28
python-dotenv>=1.0
duckduckgo-search>=5.0

# 图片处理：读群里的图 / 表情包打标 / 生图发出去前转 JPEG（甩掉工作流元数据）
Pillow>=10.0

# RAG 向量知识库（search_kb / ingest_kb / list_kb / delete_kb / chunk_document）
# 首次调用会下载 BGE-M3 模型（约 2GB），不想用知识库可以不装
chromadb>=0.5.0
sentence-transformers>=3.0
torch>=2.0
modelscope>=1.9

# 测试（tests/）用标准库 unittest 编写，无需额外依赖：
#   python -m unittest discover -s tests -v
# 若想用 pytest 运行，再单独安装：
# pytest>=7.0
